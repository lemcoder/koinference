*.iml
.gradle
.idea
.DS_Store
build
captures
.externalNativeBuild
.cxx
local.properties
xcuserdata
.kotlin
#So we don't accidentally commit our private keys
*.gpg
# Tooling / local caches
.codegraph/
.cpm/

# Prebuilt native artifacts — produced by CMake / CI, never committed
*.a

# Downloaded LiteRT-LM release archives (xcframeworks, ~44 MB each)
.prebuilt/

# JVM heap dumps, e.g. from a Gradle daemon running out of Metaspace
*.hprof

# Benchmark output: regenerated per run, and stale the moment anything is tuned.
results/
backends/cera/src/androidMain/jniLibs/
backends/cera/local-bindings/
