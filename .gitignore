*.iml
.gradle
.idea
.DS_Store
build
captures
.externalNativeBuild
.cxx
local.properties
xcuserdata
.kotlin
#So we don't accidentally commit our private keys
*.gpg
# Tooling / local caches
.codegraph/
.cpm/

# Prebuilt native artifacts — produced by CMake / CI, never committed
*.a
